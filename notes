var c = document.getElementById('can');
car ctx = dsdasd;

var makeBlock =function( x, y, h, w, ctx ) {
    return {
    	   x : x;
	   y : y;
	   sfkfk;
	   sfsd;
	   fsd;
	   draw : function() {
	   	ctx.fillstyle = this.color;
		dlad;
		};
	   move : function() {};
	   }


window.requestAnimationFrame(update); //instead of using time intervals


var blocks = [];
blocks.push